1. *Add `for` attribute to labels missing it in `admin.php`.*
   - Review labels without a `for` attribute and link them to the appropriate `<input>` `id`. Ensure uniqueness for generated elements (e.g. random generated string from JS).
2. *Review accessibility.*
   - Make sure inputs and their labels are properly linked.
3. *Complete pre commit steps.*
   - Complete pre commit steps to make sure proper testing, verifications, reviews and reflections are done.
4. *Submit the change.*
   - Once complete, I will submit the change with a descriptive commit message.
