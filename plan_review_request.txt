I need to update the application so that when there are no active online burn permits, it displays an image (provided by the user) and the text "No active online burn permits at this time." with a font size large enough to be seen from 10-20 feet away.

My proposed plan is:
1. Ensure the provided image is saved as `assets/images/no_burn_permits.png`.
2. In `index.php` and `current_index.php`, find where the "No active online burn permits at this time" message is injected (around line 1063).
3. Replace the plain `<p class="no-events">` with a new container structure:
```html
<div class="no-burn-permits">
    <img src="assets/images/no_burn_permits.png" alt="No Burn Permits">
    <p>No active online burn permits at this time.</p>
</div>
```
4. Add CSS styles in `index.php` and `current_index.php` for `.no-burn-permits`:
```css
.no-burn-permits {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
    text-align: center;
    padding: 20px;
}
.no-burn-permits img {
    max-width: 50%; /* adjust as needed */
    height: auto;
    margin-bottom: 20px;
}
.no-burn-permits p {
    font-size: 3em; /* or use clamp to scale nicely */
    font-weight: bold;
    color: var(--text-color);
}
```
5. Visually verify the changes using `verify_ui.py`.
6. Run tests and pre-commit checks.
7. Submit.

Does this plan sound reasonable?
